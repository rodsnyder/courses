# GENERATED by Local Infrastructure/bin/sync-gitleaks-ci — do not edit in this repo.
# Source: Local Infrastructure/git-hooks/ci/gitleaks.yml (config: git-hooks/gitleaks.toml).
#
# Server-side backstop for the local pre-commit gitleaks hook, which never runs for
# commits made off this Mac (cloud sessions, GitHub web edits, other machines,
# GITLEAKS_SKIP/--no-verify). Scans only the commits each push adds; a red run means
# a secret reached GitHub and must be rotated, not just removed. (backlog c866d411)
name: gitleaks

on:
  push:

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Install gitleaks 8.30.1 (checksum-pinned)
        run: |
          curl -sSfL -o gl.tgz https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz
          echo "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb  gl.tgz" | sha256sum -c -
          tar -xzf gl.tgz gitleaks

      - name: Scan commits added by this push
        env:
          BEFORE: ${{ github.event.before }}
          AFTER: ${{ github.sha }}
          REF_NAME: ${{ github.ref_name }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          set -euo pipefail
          if [ "$BEFORE" != "0000000000000000000000000000000000000000" ] && git cat-file -e "${BEFORE}^{commit}" 2>/dev/null; then
            RANGE="${BEFORE}..${AFTER}"
          elif [ "$REF_NAME" != "$DEFAULT_BRANCH" ] && git rev-parse -q --verify "origin/${DEFAULT_BRANCH}" >/dev/null; then
            RANGE="origin/${DEFAULT_BRANCH}..${AFTER}"
          else
            echo "::warning::No usable base for this push (new default branch or force-push); scanning the head commit only."
            RANGE="-1 ${AFTER}"
          fi
          echo "Scanning: $RANGE"
          ./gitleaks git --redact --no-banner --exit-code 1 --config .github/gitleaks.toml --log-opts="$RANGE" .
